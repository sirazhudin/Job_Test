Не уверен что правильно понял задание:

1. Если работать с текстовым файлом то необходимы следующие поля.
ID уникальный номер,
p.id идентификатор позиции один из Работник (W), Менеджер (М) и Административный персонал (А),
m.id идентификатор руководителя ID — т. е. Если это работник то это будет ID его начальника или 0 если нет начальника (что странно конечно же но как в задании)
start date — дата приема на работу
birth date день рождения
name
second name
surname
info — дополнительная информация.

2. Cортировка либо по полю 4(3) либо по фамилии 8(7).
3. Отбор работников по менеджеру через ID менеджера в поле m.id.
4. Удаление по полю 1(0) ID.
5. Изменение — немного сложно и неправильно но надеюсь для демонстрации подойдет.
 Строка должна быть уникальна и включает ID, p.id и m.id
 например — чтобы изменить
1,W,2,01011999,01011970,Vasia,Vasilevich,Pupkin2, good worker где 1 — это уникальный id, W — это работник и 2 это ID менеджера нужно сначала задать эту строку
а потом задать изменения — 1,W,2 - > 1,A,0 — что сделает Пупкина2 Административным работником без менеджера.

Если использовать SQL то было бы легче -
Нужно было-бы написать процедуры для поиска и сортировки и изменения и выгрузки\загрузки в файл со всеми процедурами и данными. Таблица в принципе была бы такая же, но я бы убрал поле с дополнительной информацией и поместил бы это поле в отдельную таблицу с другими полями как, зарплата, бонусы и т.п.


